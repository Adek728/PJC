#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
using namespace std;
void get_male_names_from(vector<string> v) {
    vector<string> names;
    find(v.begin(), v.end(), v.size() - 1 != "a"){
        names.push_back()
    }
}

int main() {




}
